The following file is an example JSON schema describing an Animal interface which is implemented by Dog, Cat, Fish and Human objects.

[source,json]
----
include::test-suite-generator/src/test/resources/animal.schema.json[]
----

The following test example shows how the `JsonMapper` can map the JSON data to the best fitting/correct class in the inheritance relation even though only the interface is given to the function.

[source,java]
----
include::test-suite-generator/src/test/java/io/micronaut/jsonschema/generator/animals/AnimalTest.java[tags=mapp,indent=0]
----

The following test showcases that the generated beans can be re-serialized into the same JSON Schema.

[source,java]
----
include::test-suite-generator/src/test/java/io/micronaut/jsonschema/serialization/SerializationTest.java[tags=serialization,indent=0]
----
